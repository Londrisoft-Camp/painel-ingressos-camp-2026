LUMA_API_KEY=
LUMA_CALENDAR_ID=
# Conexão direta (Neon, sem pooler). Usada por scripts de vida curta: o
# runner de migrations e o sync do GitHub Actions.
DATABASE_URL=
# Conexão via PgBouncer (Neon "pooled"). Usada pela API do painel no
# Render, que fica no ar o tempo todo e mantém pool próprio de conexões.
DATABASE_URL_POOLED=
ADMIN_USER=
ADMIN_PASSWORD=
TZ=America/Sao_Paulo
